[package]
name = "stitch-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "stitch"
path = "src/main.rs"

[dependencies]
stitch = { path = "../stitch" }
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
