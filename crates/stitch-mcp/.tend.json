{
  "version": 1,
  "node": {
    "id": "stitch-mcp",
    "description": "Build and verify the stitch-mcp MCP server",
    "tags": ["tooling", "mcp", "rust"],
    "when": {
      "changed": {
        "paths": ["**/*.rs", "Cargo.toml", ".tend.json"]
      }
    },
    "tasks": [
      {
        "id": "stitch-mcp-builds",
        "phase": "verify",
        "kind": "command",
        "command": ["cargo", "build", "-p", "stitch-mcp"]
      }
    ]
  }
}
